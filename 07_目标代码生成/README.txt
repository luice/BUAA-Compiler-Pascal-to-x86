第七次作业，目标代码生成程序。这已经是接近尾声了，最高难度的作业，是在此基础上加编译优化，如果是次高难度的，此次作业调通就算是完成了。

我生成的目标代码是 x86 汇编，这是为了学习 x86 汇编才这么选择的，否则可以选择更熟悉的 MIPS 汇编。目标代码生成程序，进行了多次验收，我一直在 Debug，这里所附的代码中很可能也是存在 bug 的。

此处有一个模块是临时寄存器池，但是此时还没做此项优化，只是简单地让目标代码生成模块调用它来获取寄存器。这是为之后真正实现临时寄存器池铺路。